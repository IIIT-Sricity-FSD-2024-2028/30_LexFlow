const cases = [{id: 1, case_type: 'Criminal', cnr: 'CNR-1234', brief_description: 'xyz', status: 'OPEN'}]; function renderCaseCard(caseObj) { const id = caseObj.id || 'N/A'; const title = caseObj.case_type || 'N/A'; const cnr = caseObj.cnr || 'N/A'; const description = caseObj.brief_description || 'N/A'; const status = caseObj.status || 'N/A'; return '<div data-title="' + title.toLowerCase() + '"></div>'; } console.log(cases.map(renderCaseCard).join(''));
